<div class="requests-search">
<%= form_tag(requests_path, method: :get, local: true, html: { class: "requests-search-bar" }) do %>
  <%= text_field_tag :search, params[:search], placeholder: "Search Requests 🔍", class: "requests-search-input" %>
  <%= submit_tag "Search" %>
<% end %>
</div>
<div class="centered-header">
  <h1>All Requests</h1>

  <% @requests.each do |request| %>
    <div class="request-form">
      <h3><%= link_to request.title, request_path(request) %></h3>

      <% if request.user.present? %>
        <p>Getter: <%= request.user.username %></p>
      <% else %>
        <p>Getter: N/A</p>
      <% end %>

      <p>Description: <%= request.description %></p>
    </div>
  <% end %>

  <div class="request-buttons">
    <%= link_to 'Back to Home Page', root_path, class: 'secondary-button' %>
  </div>
</div>
