<h1>Search Results</h1>

<% if @results.empty? %>
  <p>No search results for "<%= params[:query] %>"</p>
<% else %>
  <% @results.each do |result| %>
    <p><%= result.username %></p>
    <%= link_to 'View Profile', user_path(result), class: 'btn btn-primary button-blue' %>
  <% end %>
<% end %>
